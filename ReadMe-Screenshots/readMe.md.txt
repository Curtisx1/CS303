1. Enter an integer to search the array. If found returns the index of the value. 
2. Enter an integer to search the array. This is an index value. It then prompts for a new value for that index. Returns the old and new value. 
3. Enter an integer to add to the array. 
4. Enter an integer for index to ether remove or replace the value at chosen index with 0.